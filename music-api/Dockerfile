# TODO: Replace placeholders with a working multi-stage build.
# Example approach:
# 1) Use maven:3.9-eclipse-temurin-17 to build jar
# 2) Copy jar into eclipse-temurin:17-jre runtime image
#
# This placeholder keeps repo requirements satisfied.
FROM eclipse-temurin:17-jre
WORKDIR /app
# TODO: COPY target/*.jar app.jar
EXPOSE 8080
CMD ["sh", "-c", "echo TODO: build jar and run it; sleep 3600"]
